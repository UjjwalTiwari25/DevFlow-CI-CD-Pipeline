#
# nodeJS Dockerfile for building the application
#
# Copyright Aura Health Inc
#

FROM node:24.18.0-alpine

ARG NEXT_PUBLIC_APP_DOMAIN
ENV NEXT_PUBLIC_APP_DOMAIN=$NEXT_PUBLIC_APP_DOMAIN

ARG NEXT_PUBLIC_CDN_FIREBASE_URL
ENV NEXT_PUBLIC_CDN_FIREBASE_URL=$NEXT_PUBLIC_CDN_FIREBASE_URL

ARG NEXT_PUBLIC_CLARITY_APP_ID
ENV NEXT_PUBLIC_CLARITY_APP_ID=$NEXT_PUBLIC_CLARITY_APP_ID

ARG NEXT_PUBLIC_FIREBASE_API_KEY
ENV NEXT_PUBLIC_FIREBASE_API_KEY=$NEXT_PUBLIC_FIREBASE_API_KEY

ARG NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
ENV NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN

ARG NEXT_PUBLIC_FIREBASE_DATABASE_URL
ENV NEXT_PUBLIC_FIREBASE_DATABASE_URL=$NEXT_PUBLIC_FIREBASE_DATABASE_URL

ARG NEXT_PUBLIC_FIREBASE_PROJECT_ID
ENV NEXT_PUBLIC_FIREBASE_PROJECT_ID=$NEXT_PUBLIC_FIREBASE_PROJECT_ID

ARG NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
ENV NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET

ARG NEXT_PUBLIC_MIXPANEL_TOKEN
ENV NEXT_PUBLIC_MIXPANEL_TOKEN=$NEXT_PUBLIC_MIXPANEL_TOKEN

ARG NEXT_PUBLIC_BUGSNAG_KEY
ENV NEXT_PUBLIC_BUGSNAG_KEY=$NEXT_PUBLIC_BUGSNAG_KEY

ARG NEXT_PUBLIC_LOGGER_LEVEL
ENV NEXT_PUBLIC_LOGGER_LEVEL=$NEXT_PUBLIC_LOGGER_LEVEL

ARG NEXT_PUBLIC_LOGGER_USE_JSON
ENV NEXT_PUBLIC_LOGGER_USE_JSON=$NEXT_PUBLIC_LOGGER_USE_JSON

ARG NEXT_PUBLIC_AURA_SERVICES_URL
ENV NEXT_PUBLIC_AURA_SERVICES_URL=$NEXT_PUBLIC_AURA_SERVICES_URL

ARG NEXT_PUBLIC_STRIPE_KEY
ENV NEXT_PUBLIC_STRIPE_KEY=$NEXT_PUBLIC_STRIPE_KEY

ARG NEXT_PUBLIC_AURA_WEB_URL
ENV NEXT_PUBLIC_AURA_WEB_URL=$NEXT_PUBLIC_AURA_WEB_URL

ARG NEXT_PUBLIC_BRANCH_KEY
ENV NEXT_PUBLIC_BRANCH_KEY=$NEXT_PUBLIC_BRANCH_KEY

ARG NEXT_PUBLIC_PIXEL_ID
ENV NEXT_PUBLIC_PIXEL_ID=$NEXT_PUBLIC_PIXEL_ID

ARG NEXT_PUBLIC_PIXEL_ANDROID_ID
ENV NEXT_PUBLIC_PIXEL_ANDROID_ID=$NEXT_PUBLIC_PIXEL_ANDROID_ID

ARG NEXT_PUBLIC_PIXEL_IOS_ID
ENV NEXT_PUBLIC_PIXEL_IOS_ID=$NEXT_PUBLIC_PIXEL_IOS_ID

ARG NEXT_PUBLIC_GEOLOCATION_DB_KEY
ENV NEXT_PUBLIC_GEOLOCATION_DB_KEY=$NEXT_PUBLIC_GEOLOCATION_DB_KEY

ARG NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID
ENV NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID=$NEXT_PUBLIC_GOOGLE_TAG_MANAGER_ID

ARG NEXT_PUBLIC_GOOGLE_CALENDAR_AUTH_CLIENT_ID
ENV NEXT_PUBLIC_GOOGLE_CALENDAR_AUTH_CLIENT_ID=$NEXT_PUBLIC_GOOGLE_CALENDAR_AUTH_CLIENT_ID

ARG NEXT_PUBLIC_GOOGLE_CLIENT_ID
ENV NEXT_PUBLIC_GOOGLE_CLIENT_ID=$NEXT_PUBLIC_GOOGLE_CLIENT_ID

ARG NEXT_PUBLIC_USER_WEB_TOKEN
ENV NEXT_PUBLIC_USER_WEB_TOKEN=$NEXT_PUBLIC_USER_WEB_TOKEN

ARG NEXT_PUBLIC_TIKTOK_PIXEL_ID
ENV NEXT_PUBLIC_TIKTOK_PIXEL_ID=$NEXT_PUBLIC_TIKTOK_PIXEL_ID

ARG PACKAGE_AUTH_TOKEN
ENV PACKAGE_AUTH_TOKEN=$PACKAGE_AUTH_TOKEN

# Create app directory
WORKDIR /home/app

# Copy the package json
COPY package.json package-lock.json /home/app/

# Copy the npmrc file
COPY .npmrc /home/app

# Install the libraries
RUN npm install --quiet --production

# Copy the source code
COPY . /home/app

RUN npm run build

EXPOSE 3000
CMD [ "npm", "run", "serve" ]
